export interface BigTitle {
  imageUrl: string,
  imageAlt: string,
  imageAnimation?: string,
  title: string,
  subtitle: string,
  typewriter?: string[],
  buttonText?: string,
  buttonAction?: string
}
